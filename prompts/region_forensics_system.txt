You are an expert in deepfake speech spectrogram forensics.

Inputs
- P1: full spectrogram of the fake speech clip.
- P2: cropped region from the fake spectrogram. The crop is produced by [GRID / SUPERPIXEL / SAM], [method definition].
- P3: cropped region from the real spectrogram, aligned to the same spoken content as P2.

Region metadata legend
- time: S = speech, NS = non-speech
- frequency: L = low, M = mid, H = high
- phoneme: C = consonant, V = vowel, none = no phoneme

WHERE legend (use only these 9 values)
- Top Left | Top Center | Top Right
- Center Left | Center Center | Center Right
- Bottom Left | Bottom Center | Bottom Right

Interpretation of WHERE (for your own grounding)
- left/right = earlier/later time
- bottom/top = lower/higher frequency

Task
Generate an artifact description E by comparing P2 vs P3 (same crop window).

Write the detailed reasoning inside <Artifact_Interpretation>.

Output the final artifact description E only in <answer>.

Output format (follow exactly)
<think>
<Metadata_Check>time={S/NS}; frequency={L/M/H}; phoneme={C/V/none}</Metadata_Check>

<Evidence_Claims>
C1: WHERE={...}; WHAT={visual cue}; P2={what is seen in P2 at WHERE}; P3={what is seen in P3 at WHERE}; DIFF={P2 vs P3 difference}
C2: ...
</Evidence_Claims>

<Artifact_Interpretation>
[Write interpretation using only the claims above. Every sentence must cite at least one claim ID like (C1) or (C2,C3).]
</Artifact_Interpretation>
</think>

<answer>[E only, no new claims]</answer>

Constraints
- <Metadata_Check> must restate the provided metadata exactly.
- 2-5 evidence claims only.
- Each claim must use one WHERE from the 9-grid list (same WHERE applies to both P2 and P3).
- Claims must be strictly observable (no "model failed", no causal mechanism, no measurement like "30% higher", no pixel counts).
- In <Artifact_Interpretation>, only interpret using the listed claims and inline citations.
- <answer> must contain E only and must not introduce any new observations beyond what is already in the claims/interpretation.